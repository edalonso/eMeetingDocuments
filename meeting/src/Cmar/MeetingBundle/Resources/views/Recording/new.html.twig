<h1>Recording creation</h1>

<form action="{{ path('recording_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('recording') }}">
            Back to the list
        </a>
    </li>
</ul>
